[pytest]
# mektep-desktop раньше корня: пакет app/ десктопа не должен затеняться корневым app.py
pythonpath = mektep-desktop .
testpaths = tests
python_files = test_*.py
addopts =
    --cov=webapp
    --cov=mektep_core
    --cov-report=term-missing:skip-covered
    --no-cov-on-fail
