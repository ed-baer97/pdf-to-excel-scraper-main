worker_processes auto;
pid /var/run/nginx.pid;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile        on;
    tcp_nopush      on;
    tcp_nodelay     on;
    keepalive_timeout  65;
    types_hash_max_size 2048;

    # Gzip Settings
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Rate limiting zones
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    # Экспорты Excel — тяжёлые фоновые задачи; жёсткий лимит на запуск
    limit_req_zone $binary_remote_addr zone=export_limit:10m rate=1r/s;

    upstream mektep_app {
        server web:5000;
        keepalive 32;
    }

    server {
        listen 80;
        server_name _;

        # Max upload size (for file uploads)
        client_max_body_size 50M;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # Static files (if any)
        location /static/ {
            alias /app/webapp/static/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        # Desktop app updates (installer + latest.json)
        location = /updates/latest.json {
            alias /var/www/mektep/updates/latest.json;
            add_header Cache-Control "no-cache, no-store, must-revalidate";
        }

        location /updates/ {
            alias /var/www/mektep/updates/;
            autoindex off;
        }

        # Health check endpoint
        location /health {
            proxy_pass http://mektep_app/health;
            access_log off;
        }

        # API endpoints with rate limiting
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            limit_req_status 429;
            
            proxy_pass http://mektep_app;
        }

        # Admin exports: создание задачи экспорта — тяжёлая операция.
        # Точный матч: /admin/exports/<id>/status (частый поллинг) не ограничиваем.
        location = /admin/exports {
            limit_req zone=export_limit burst=5 nodelay;
            limit_req_status 429;

            proxy_pass http://mektep_app;
        }

        # Main application
        location / {
            proxy_pass http://mektep_app;
        }
    }

    # HTTPS server (uncomment and configure for production)
    # server {
    #     listen 443 ssl http2;
    #     server_name your-domain.com;
    #
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     ssl_session_timeout 1d;
    #     ssl_session_cache shared:SSL:50m;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256';
    #     ssl_prefer_server_ciphers on;
    #
    #     # HSTS
    #     add_header Strict-Transport-Security "max-age=31536000" always;
    #
    #     # Same location blocks as above
    # }
}
